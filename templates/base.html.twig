<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <meta name="description" content="VillageGreen - Votre boutique en ligne pour instruments de musique, accessoires et équipements audio.">
        <title>{% block title %}VillageGreen - La musique à portée de main{% endblock %}</title>
        <link rel="icon" href="{{ asset('favicon.ico') }}" type="image/x-icon">
        
        <!-- Bootstrap CSS -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
        
        <!-- Bootstrap Icons -->
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">
        
        {% block stylesheets %}
            <link href="{{ asset('css/style.css') }}" rel="stylesheet">
        {% endblock %}
    </head>
    <body class="d-flex flex-column min-vh-100">
        <!-- Navbar -->
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark bg-opacity-75">
            <div class="container-fluid">
                <a class="navbar-brand" href="{{ path('index') }}">
                    <i class="bi bi-music-note-beamed"></i> VillageGreen
                </a>
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav">
                        <li class="nav-item">
                            <a class="nav-link {% if app.request.get('_route') == 'index' %}active{% endif %}" href="{{ path('index') }}">
                                <i class="bi bi-house-door"></i> Accueil
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link {% if app.request.get('_route') == 'catalogue' %}active{% endif %}" href="#">
                                <i class="bi bi-music-note-list"></i> Catalogue
                            </a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link {% if app.request.get('_route') == 'panier' %}active{% endif %}" href="#">
                                <i class="bi bi-cart"></i> Panier
                            </a>
                        </li>
                        {% if app.user %}
                            <li class="nav-item">
                                <a class="nav-link" href="#">
                                    <i class="bi bi-person-circle"></i> Mon Profil
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="#">
                                    <i class="bi bi-box-arrow-right"></i> Déconnexion
                                </a>
                            </li>
                        {% else %}
                            <li class="nav-item">
                                <a class="nav-link" href="#">
                                    <i class="bi bi-box-arrow-in-right"></i> Connexion
                                </a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="#">
                                    <i class="bi bi-person-plus"></i> Inscription
                                </a>
                            </li>
                        {% endif %}
                    </ul>
                    <form class="d-flex ms-auto" action="#" method="get">
                        <input class="form-control me-2" type="search" name="q" placeholder="Rechercher un produit" aria-label="Search">
                        <button class="btn btn-outline-light" type="submit">
                            <i class="bi bi-search"></i>
                        </button>
                    </form>
                </div>
            </div>
        </nav>

        <!-- Main Content -->
        <main class="flex-grow-1">
            <div class="container">
                {% block body %}{% endblock %}
            </div>
        </main>

        <!-- Footer -->
        <footer class="transparent-footer text-center text-lg-start text-white mt-auto">
            <div class="text-center p-3">
                <p>
                    <i class="bi bi-music-note"></i> "La musique donne une âme à nos cœurs et des ailes à la pensée." - Platon
                </p>
                <a href="/privacy-policy" class="text-light">Politique de confidentialité</a> |
                <a href="/terms" class="text-light">Conditions d'utilisation</a>
                <div class="mt-2">
                    <a href="https://facebook.com" class="text-light me-2"><i class="bi bi-facebook"></i></a>
                    <a href="https://twitter.com" class="text-light me-2"><i class="bi bi-twitter"></i></a>
                    <a href="https://instagram.com" class="text-light"><i class="bi bi-instagram"></i></a>
                </div>
            </div>
        </footer>

        <!-- Bootstrap JS Bundle -->
        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js" defer></script>
        
        {% block javascripts %}
        {% endblock %}
    </body>
</html>
